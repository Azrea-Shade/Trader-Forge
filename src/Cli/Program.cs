using System;

namespace Cli
{
    internal static class Program
    {
        public static int Main(string[] args)
        {
            Console.WriteLine("Trader-Forge CLI stub: briefing workflow not wired in this build.");
            // TODO: Re-enable when BriefingService public API (ctor + methods) is finalized.
            return 0;
        }
    }
}
