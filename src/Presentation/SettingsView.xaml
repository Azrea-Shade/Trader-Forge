<Window x:Class="Presentation.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Width="420" Height="280"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource App.Background}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Daily Brief" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Generate at (HH:mm)" Width="160" VerticalAlignment="Center"/>
                <TextBox Text="{Binding GenerateAt, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Notify at (HH:mm)" Width="160" VerticalAlignment="Center"/>
                <TextBox Text="{Binding NotifyAt, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
            </StackPanel>
            <CheckBox Content="Auto-start with Windows" IsChecked="{Binding Autostart}" Margin="0,4,0,0"/>
            <CheckBox Content="Enable Windows Toasts" IsChecked="{Binding EnableToasts}" Margin="0,4,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="90" IsDefault="True" Command="{Binding SaveCmd}"/>
        </StackPanel>
    </Grid>
</Window>
