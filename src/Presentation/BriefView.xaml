<UserControl x:Class="Presentation.BriefView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Daily Brief"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,8"/>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- These bindings are runtime-only; safe even if the properties are null/missing -->
                <TextBlock Text="{Binding GeneratedAtLocal, StringFormat=Generated: {0:HH:mm}}"
                           Margin="0,0,0,4"/>
                <TextBlock Text="{Binding SummaryText}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,8"/>

                <ItemsControl ItemsSource="{Binding Tickers}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
