<UserControl x:Class="Presentation.BriefView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16" RowDefinitions="Auto,*,Auto" RowSpacing="12">
        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="Daily Brief" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="(Today)" Opacity="0.7" Margin="8,0,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Padding="12" CornerRadius="8"
                BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}">
            <ScrollViewer>
                <TextBox Text="{Binding Content}" IsReadOnly="True"
                         TextWrapping="Wrap" BorderThickness="0"
                         FontFamily="Consolas" MinHeight="220"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button Content="Generate now" Command="{Binding GenerateNow}" />
            <Button Content="Mark delivered" Command="{Binding MarkDelivered}" />
            <TextBlock Text="{Binding Status}" Margin="8,4,0,0" Opacity="0.7"/>
        </StackPanel>
    </Grid>
</UserControl>
