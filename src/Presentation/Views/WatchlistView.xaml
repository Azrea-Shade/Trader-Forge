<UserControl x:Class="Presentation.Views.WatchlistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Background="#0b0f1a" Margin="8">
    <StackPanel>
      <TextBlock Text="{Binding Header}" FontSize="20" Foreground="White" FontWeight="Bold"/>
      <TextBlock Text="{Binding Note}" Foreground="#DDFFFFFF" Margin="0,6,0,0"/>
    </StackPanel>
  </Grid>
</UserControl>
